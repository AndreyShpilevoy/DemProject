import en from "./en";
import ru from "./ru";
import uk from "./uk";

const rootTranslations = [
  {locale: "en", translationArray: en},
  {locale: "ru", translationArray: ru},
  {locale: "uk", translationArray: ru}
];

export default rootTranslations;
